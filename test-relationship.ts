import { calculateBazi } from './src/services/bazi-service.js';
import { calculateRelationshipDynamics } from './src/services/relationship-dynamics-service.js';

const run = async () => {
    // 93-02-26 10:40 M
    // 90-11-16 14:00 F
    
    // We cannot easily run TS without compiling, but let's test building app and checking logs!
    console.log("ready");
}
// since the import paths are complex and ES modules are needed, I'll just rely on starting the server and making a fetch manually or writing to console on start.
